name: calendar_scheduler
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  table_calendar: 3.1.2 # 달력 기능
  intl: 0.19.0 # 다국어화 기능
  drift: 2.21.0 # Drift ORM
  drift_flutter: 0.1.0 # SQLite 데이터베이스
  path_provider: 2.1.5 # 경로 관련 기능
  path: 1.9.1 # 경로 관련 기능
  get_it: 8.0.2 # 프로젝트 전역으로 의존성 주입을 가능하게 함
  dio: 5.7.0 # 네트워크 요청을 가능하게 함
  provider: 6.1.2 # 글로벌 상태 관리를 가능하게 함Í›
  uuid: 4.5.1 # UUID 생성 할 수 있게함
  firebase_core: ^3.15.0 # 파이어베이스 기본 플러그인
  cloud_firestore: 5.4.4 # 파이어 스토어 플러그인

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  drift_dev: 2.21.0 # Drift 코드 생성 기능 관련
  build_runner: 2.4.13 # 플러터에서 코드 생성 기능을 제공해줌

flutter:

  uses-material-design: true